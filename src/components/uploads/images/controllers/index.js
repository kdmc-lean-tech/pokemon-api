const {
  uploadAvatarPokemonController
} = require('./upload-avatar-pokemon/upload-avatar-pokemon.controller');

const {
  uploadAvatarUserController
} = require('./upload-avatar-user/upload-avatar-user.controller');

module.exports = {
  uploadAvatarPokemonController,
  uploadAvatarUserController
}
